# OpsNow Copilot - Knowledge PoC 환경 설정 예시
# 로컬 개발 환경에서는 .env.local 파일로 복사하여 사용하세요
# cp .env.example .env.local

# =============================================================================
# 로컬 개발 환경 설정 (필수)
# =============================================================================
# LOCAL_DEV=true 설정 시:
#   - Swagger docs 활성화
#   - 디버그 로깅 활성화
LOCAL_DEV=true

# =============================================================================
# OpenAI API 설정 (필수)
# =============================================================================
OPENAI_API_KEY=sk-your-api-key-here

# =============================================================================
# Redis 설정 (필수)
# =============================================================================
REDIS_URL=redis://localhost:6379

# Redis 타임아웃 (초, 기본: 5)
# REDIS_SOCKET_CONNECT_TIMEOUT=5
# REDIS_SOCKET_TIMEOUT=5

# =============================================================================
# 파일 경로 설정 (선택)
# =============================================================================
# 임베딩 벡터 파일 경로 (기본: src/data/output/{EMBEDDING_MODEL}/embedded_vector.pkl)
# EMBEDDED_VECTOR_PATH=src/data/output/text-embedding-3-small/embedded_vector.pkl

# =============================================================================
# LLM 모델 설정 (선택)
# =============================================================================
# OpenAI 모델 (기본: gpt-5-nano)
# Agent, Rerank, Summarizer, Tagging 모두 동일 모델 사용
# OPENAI_MODEL=gpt-5-nano

# Embedding 모델 (기본: text-embedding-3-small)
# EMBEDDING_MODEL=text-embedding-3-small

# =============================================================================
# 로깅 설정 (선택)
# =============================================================================
# 로그 레벨 (기본: INFO)
# 값: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO
